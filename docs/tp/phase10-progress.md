# Phase 10 progress — two-slot eager continuous-batching integration

Status: Phase 10 Stage-2 TP=2 prefill/decode overlap accepted on the two-Spark fixture with device sampling and unified scheduling enabled; the final evidence and scope are at the end of this file. Earlier gate inventories below are historical.

Rank 0 retains the existing engine scheduler, admission, cancellation, and sampler. A `--max-batch 2` TP=2 run elects the ordinary batched loop rather than the Phase 9 serial loop. The scheduler's dense high-water rows are translated to ordered active slot/token/position rows; holes are omitted and logits are returned at the original slot indices. Prefill uses the generator's per-prompt default (eager token steps), and each decode row runs the accepted whole-model rank-local TP primitives in the same order on both ranks. This is continuous admission and batched *scheduler* execution, not a fused GPU batch or nonblocking forward.

Rank 0 authorizes logical paged-KV Ensure/Release/Flush events; rank 1 mirrors and checks sequence, slot order, position and resulting KV snapshot before its next collective. A slot release resets its DeltaNet recurrent/conv state without touching surviving slots; full reset clears every slot. Each GQA layer has slot-indexed block tables and rank-local payload. Both ranks retain Phase 9 pinned checkpoint/pack/context bootstrap, FP16 KV, eager kernels and two-rank NCCL collectives. TP graph capture, speculation, vision, offload and companion models remain excluded.

Host checks: `cargo check -p paddock-runner --all-targets`; `cargo test -p paddock-engine --lib` (422 passed); `cargo test -p paddock-runner --lib` (506 passed); `cargo test -p paddock-dist` (18 integration tests passed); `git diff --check`. New host tests cover sparse slot membership, mirrored two-slot release/reuse with a surviving slot, and ordered prepared/ready acknowledgements.

Two-Spark smoke (release runner SHA-256 `eba836b6d65bd048c925441a1c2a2b0769c0a1b9ba660bbb2e8dec6ef719db26` verified on both nodes, pinned GGUF SHA-256 `322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482`, pack SHA-256 `059bb62d2e6d863b32ac47208d29da7492618b2fa33015a3ee0ad6fe2a9c4d54`): max_ctx=48, max_batch=2, no speculation, FP16 KV, RoCE interface pins as in Phase 9. Concurrent `Hi`/`Hello` completions each returned `", I'm a 2"` with 1 prompt and 6 completion tokens. A streamed `Hi` request was closed after its first token while a 14-token prompt continued to 18 completion tokens across a logical KV page; the survivor returned `"\n\nThis is a classic example of a **pangram**—a sentence that contains every"`. Two concurrent 4-token requests after the cancellation each returned `", I'm a"`. A separately loaded TP=1 server with the same GGUF and pack reproduced the complete (non-cancelled) text and token counts for those prompts. Both TP=2 ranks and the TP=1 server exited 0 after graceful shutdown. This is HTTP text/count parity, not proof of identical token IDs, all logits, precise cancellation timing, or async overlap. An earlier launch failed closed on a stale serial-only load guard; it was fixed before the successful run.

A second two-Spark run with two 32-token greedy requests sampled `/metrics` during execution: `paddock_num_requests_running` reached 2 (observed values 0 and 2), both requests completed, and both ranks exited 0. This proves two scheduler slots were occupied concurrently, but the gauge alone cannot prove each observed instant was a fused GPU decode batch.

Two-Spark eager numerical gate: `crates/paddock-engine/examples/qwen35_two_slot_oracle.rs` (same pinned GGUF/pack hashes, MAX_CTX=48, FP16 KV, TP graphs/speculation disabled) exited 0 on **both** ranks. Rank 0 authored ordered slot/token/position/KV events; rank 1 checked and replayed them. The script admitted both slots, interleaved rows, cancelled slot 1 after its decode row while slot 0 continued, released/reset that slot on both ranks, readmitted a new request at position 0, and carried the survivor across the 16-token KV page boundary. Every vocabulary logit was bit-identical between ranks and on full-script reset/replay. A third, isolated slot-0 pass was bit-identical to all 21 corresponding interleaved rows, including after cancellation. Each logical request's complete token trace was separately replayed against an independent TP=1 full-weight GPU model: greedy IDs matched every row; max observed absolute full-vocabulary difference was 0.00803137 at survivor position 13. A strict `1e-3 + 1e-3*|reference|` check first failed from survivor position 9; a separate single-slot TP=2 run reproduced the **same** differences, ruling out inter-slot interference as their cause. The oracle retains exact rank/replay/interleaving assertions and uses a documented `1e-2 + 1e-3*|reference|` TP=1 tolerance for accumulated numerical differences. This is an eager-GPU script, not proof that the production scheduler takes the same cancellation boundary or that asynchronous stages are correct.

Device-sampling implementation in the current uncommitted tree: rank 0's TP model now uses the existing `sample_rows` kernel on its resident logits for eligible Greedy/Categorical plans and reads back one token ID per device row; Host rows still read full logits, and scheduler holes produce neither. The rank-0 proxy carries the scheduler-authored `RowSample` plans separately from the ordered two-rank token/position/KV protocol; it still waits for `TpPrepared` and `TpReady` before publishing results or releasing a slot. Unsupported sampling plans fail closed; the runtime pack must expose `sample_rows` before the generator advertises this capability. `PADDOCK_NO_GPU_SAMPLE` retains the scheduler's host fallback. Checks run after the sampled path and fail-closed logging: `cargo test -p paddock-engine --lib` (424 passed), `cargo test -p paddock-runner --lib` (506 passed), `cargo test -p paddock-dist` (18 integration tests passed), `cargo check -p paddock-runner --all-targets`, `cargo check -p paddock-engine --example qwen35_tp_sampled`, release example build, and `git diff --check`. The two-rank production-generator GPU probe `qwen35_tp_sampled.rs` was transferred with matching executable SHA-256 `4c13f4a41f32cf39b2fb98dad24984901755c756fef47eb36ab7970f65daa448` and exited 0 on both Sparks with the pinned checkpoint/pack: device greedy ID 151 matched the prior full-logit argmax, the mixed Host row matched every previous logit bit-for-bit, released slot 1 was reused while slot 0 survived, and a sparse slot-0 hole returned no row. A final expanded probe, executable SHA-256 `13de2158494387b12f4437269133ccb0d251af17df67de571a3b8b6795075a68` matched across hosts and exited 0 on both ranks: fixed-u categorical replay survived a changed slot-1 history, and the live slot continued with device-sampled IDs through position 20, across the 16-token logical KV page while slot 0 was a hole. Two-rank HTTP requests (greedy `Hi`, categorical `Hello`) each returned ten completion tokens; an otherwise identical restart with `PADDOCK_NO_GPU_SAMPLE=1` returned the same text and token counts for both requests. This is a sampled production-generator and scheduler smoke, not evidence of categorical distribution parity or nonblocking completion. The `Generator` release/reset hooks return no error, so the TP proxy records and logs a poisoned rank pair if either fails; subsequent forwards fail rather than silently reusing unacknowledged state.

Model-level device-token feedback seam (uncommitted): `Qwen35TpRank` now has two device ID planes per slot; rank 0 samples a host-token row into one plane, then both ranks enter a fenced rank-0 NCCL broadcast before consuming that device ID as the next token and rank 0 samples into the alternate plane. The coordinator can poll an event and read an old plane via the existing copy-stream helper after queuing the next step. `qwen35_tp_feedback.rs`, release executable SHA-256 `bec2fdd271df3eab78510869ae2a7340e67c241e8ee719a6f4d10f5528345b17` matching on both Sparks, exited 0 on both ranks: six device-feedback greedy IDs matched a reset eager full-logit replay. `cargo check -p paddock-engine --example qwen35_tp_feedback`, release build, `cargo test -p paddock-engine --lib` (425 passed), `cargo check -p paddock-runner --all-targets`, and `git diff --check` passed. This validates the model primitive, not the production protocol, scheduler pipe or nonblocking completion.

## Earlier gate inventory (historical)

1. Establish sampled-path acceptance beyond smoke: assert categorical device results against the existing device sampler for fixed draws and a range of logits, verify rank-0 RNG consumption/hole behavior under the scheduler, and exercise cancellation/reuse and KV page crossing in the production sampled path. The HTTP text/count fallback comparison is not token-ID or distribution proof.
2. A bounded production-generator TP=2 pure-decode pipe is implemented and device-probed: ordered rank-0 KV authorization, worker Prepared before collectives, device-feedback ID broadcast, alternating per-slot planes, next-tick enqueue before prior-ID readback, deferred Ready, and both-rank compute/NCCL drain before release/reset/reuse. `qwen35_tp_pipe` matched all eager greedy IDs over positions 2–20 for two slots, crossed the logical page, then drained/released/reused a slot and reset/replayed; both ranks exited 0. The release probe executable SHA-256 `f6649ceef5b4a1b8f90e3eee9979fe919cd7a260407c8485e374bac401a61ba6` matched on both nodes; checkpoint SHA-256 `322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482` and pack SHA-256 `059bb62d2e6d863b32ac47208d29da7492618b2fa33015a3ee0ad6fe2a9c4d54` matched. The live scheduler token-ID/RNG gate was subsequently closed as documented below; `supports_decode_pipe` now advertises the production capability when device sampling is available. This does not imply nonblocking prefill/decode overlap.
3. Implement and verify nonblocking completion and genuine prefill/decode overlap using Paddock's unified-span, chunked-prefill and slot-mapped pipe contracts, separate stream-owned scratch and explicit cross-lane fences. The CUDA pack's large-vocabulary `sample_rows` uses process-global `pd_sr_*` scratch (`stage2_sample.cuh:2239-2249,2439-2455`); simultaneous calls from independent streams can race unless sampling is serialized or the scratch becomes lane-local. Do not advertise `supports_overlap` merely for sequential prefill/decode. A TCP ACK timeout does not protect a stalled collective.

## Live scheduler pipe acceptance

The scheduler bounds fixed-context lookahead against `max_ctx`, including completed members still executing as dummies. `TpGenerator::decode_pipe_context_limit()` supplies the capacity; a host test covers limit, hole, and dummy positions. Unenqueued plans peek categorical uniforms; successful enqueue commits a draw only for a request surviving the preceding token, so dummy overshoot and abandoned arrivals do not advance a cancelled/completed seed. TP prefill remains after pipe drain because the rank owns one model/stream/scratch lane; the short-admission-before-drain shortcut now requires `supports_overlap`.

The initial two-Spark HTTP pipe probe (release runner SHA-256 `8fe0aef3324c92fa8c52b14df8d8196d3b852f35b6a821fcc3030748290ed57d`) compared context exhaustion, a short completed dummy beside a surviving long slot, streamed cancellation, reuse and arrival against sampled eager. Both ranks exited 0, and response text, finish and usage matched. This was an HTTP-only gate, not token-ID proof.

The subsequent live token-ID/RNG gate used a temporary per-request scheduler trace (removed after validation) with explicit `top_k=0`, `top_p=1`, `min_p=0` to exercise device categorical rather than the checkpoint's host-truncation defaults. Final traced runner SHA-256 `fe5377683505e8eb13674d85e27b4afaa11935b9d6006fce014a8f4dbd8e47ee` matched on both Sparks, as did GGUF SHA-256 `322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482` and pack SHA-256 `059bb62d2e6d863b32ac47208d29da7492618b2fa33015a3ee0ad6fe2a9c4d54`. The probe covered the prior fixed-seed/greedy scenarios plus six reproducibly randomized arrival/cancellation/reuse cases (Python RNG seed 541). Across 20 distinct request seeds (excluding the startup warmups), all 343 scheduler-sampled token IDs matched sampled eager exactly, including 208 samples while 14 real pipe segments were in flight. The sampled-eager and pipe traces also agreed on the next categorical plan/uniform at every matching token after moving the speculative next-plan commit behind the previous-token finish check (343/343); before this correction, in-pipe trace points were a draw ahead. All comparable HTTP response fields matched and both ranks exited 0 in both runs. Cancelled requests produced two sampled tokens in each run, but the test does not claim an identical wall-clock cancellation boundary in general. Scratch evidence: `~/.hermes/cache/scratch/phase10-{tracepipe-final,traceeager-final}-{head,worker}.log`, corresponding `*-results.json`, and `phase10_live_scheduler.py`.

With the temporary probe and trace switches removed, `supports_decode_pipe` now follows the sampled-device capability. The final default release runner SHA-256 `1ad61935361a377708cb95298f1a599658ed13909f2922d9f008d8de21c24dc6` matched on both hosts; the same live suite exited 0 on both ranks, matched sampled-eager HTTP response fields, and logged 14 begin/14 drain segments with no explicit pipe switch. Host verification: 429 engine library tests, 506 runner library tests, 18 distributed integration tests, runner all-targets check, release runner build, and `git diff --check` passed. `supports_overlap` stays false. TP graphs, speculation and offload remain out of scope.

## Stage-2 overlap continuation (interim host evidence)

The dirty stage-2 tree now builds and passes host checks after fixing `prefill_lane_mark`'s `GpuError` conversion and concrete protocol bugs found during continuation: rank 0 consumes the span-launch tail `TpReady` after enqueue (before either a mapped pipe begin or span finish expects a later ACK), rank 1 validates multirow `TpMixed`/`TpSpanLaunch` positions against an advancing cursor before `TpPrepared`, and the shared span-done flag now reflects the prefill GPU event instead of remaining false for the entire host-side flight. These ACKs acknowledge enqueue only; the explicit lane/NCCL joins remain the GPU fences. `supports_overlap` is explicitly false pending simultaneous-flight acceptance. `engine_finisher_plan` no longer exists; the current symbol is `wire_finisher_plan` in `crates/paddock-engine/src/gpu_model/qwen35/tp_serve.rs`, which remains used to serialize rank-0 finisher plans.

Host evidence on this tree: `cargo test -p paddock-engine -p paddock-runner -p paddock-dist --offline` exited 0 (430 engine library, 506 runner library and 18 distributed integration tests); the added host test rejects repeated/skipped/out-of-range multirow positions before Prepared. `cargo check --all-targets --offline` exited 0; `git diff --check` exited 0. The `chunk_rows` binding is already immutable and `finishers` in `forward_mixed` must remain mutable because its plan is assigned in the following loop; no unused-mut warnings appeared in the host check. Release runner SHA-256 `b6673ad69efd552e34e520367f3fc94d967ddba44bb88f2239e7a20d1b0a2983` matched on both Sparks; checkpoint SHA-256 `322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482` and CUDA pack SHA-256 `059bb62d2e6d863b32ac47208d29da7492618b2fa33015a3ee0ad6fe2a9c4d54` matched. The existing `phase10_live_scheduler.py` suite with `PADDOCK_UNIFIED=1` and the normal pipe passed and both rank processes exited 0 (`~/.hermes/cache/scratch/phase10-stage2-verified-{head,worker}.log`, `phase10-stage2-verified-results.json`). Because the overlap capability is gated false, this is a two-rank decode-pipe regression only. It does not establish simultaneous GPU flights, final-row/token/RNG oracle parity, or safe reuse after concurrent lane work. Those target gates remain open.

## Historical overlap audit and implementation boundary (superseded by Stage-2 work above)

The following inventory describes the pre-Stage-2 implementation; see the live bring-up section for current status.

The detailed dependency-ordered design and acceptance gates are in [phase10-overlap-boundary.md](phase10-overlap-boundary.md). This is a clean design boundary, not an implemented overlap lane.

The current model has one compute stream and one `token`/`x`/`xn`/`logits`/sampling-parameter scratch set (`tp_model.rs:56-76,216-235,445-545`); the two ID planes protect only feedback. The layer paths also reuse their own scratch: FFN gate/up/partial/reduced (`ffn_tp.rs:36-46,109-127`), GQA projections/metadata/block table (`gqa_tp.rs:92-113,308-508`), and DeltaNet intermediates plus slot-swapped recurrent/conv state (`delta_tp.rs:200-259,435-456,468-629`). A second compute stream without distinct lane scratch would race even on disjoint slots. The large-vocabulary CUDA sampler explicitly uses device-global `pd_sr_*` arrays across its A/B/C kernels (`packs/cuda/src/deltanet/stage2_sample.cuh:2230-2249,2439-2455`); concurrent rank-0 sampler calls need caller-owned scratch or an explicit cross-stream sample fence. NCCL already has its own stream with compute/NCCL event ordering (`gpu/distributed.rs:108-182`), but those fences currently refer only to the model's one compute stream.

The TP prefill implementation is a serial one-token `run_rows` loop with full-logit readback on every prompt token (`tp_serve.rs:281-293,505-520`); no TP unified-span/chunked-prefill trait implementation exists. The worker and coordinator reject non-pipe commands until drain (`tp_serve.rs:671-674,948-955`), and the existing TP wire messages have no independent prefill flight/lane identity. The scheduler requires a unified span and arbitrary-slot pipe membership together for its overlap branch (`service.rs:3781-3873`). The minimum viable path is therefore: separate per-lane backbone and per-layer scratch while sharing immutable weights and slot-owned KV/state; implement a bounded TP prefill span and final-row finisher; extend the rank-0-authorized protocol for independent lane/flight identities and mirrored KV events while preserving Prepared-before-collectives and deferred Ready; add explicit compute/NCCL/cross-lane/sample fences and join both ranks before release/reset/reuse; then prove simultaneous in-flight work and token/RNG/logit parity on both GPUs across cancellation, page crossing and slot reuse. Do not enable `supports_overlap` on host-level interleaving alone.

## Stage-2 live overlap bring-up (interim probe evidence)

`TpGenerator::load` advertised `overlap: false`, so the earlier `PADDOCK_UNIFIED=1` run could not enter the span branch. A temporary `PADDOCK_TP_OVERLAP_PROBE` capability switch, gated by device sampling, now reaches the existing span path without enabling the default capability. The live two-Spark scheduler suite (`phase10_live_scheduler.py`) exited 0 on both ranks with 10 span launches. A variant in `~/.hermes/cache/scratch/phase10_overlap_long.py` uses 191-token arriving prompts, max_ctx=256, fixed-seed categorical decoding, streaming cancellation, boundary crossing, and reuse; both ranks also exited 0. The release executable hash for the initial run was `c0d5490fe77bd9ef20b9cea3143d884edfc477dece3f9fb7b7deb05017241de2`; after the rank-1 event fix it was `52c20a6a83f6a782a9694c8f98ab550c4ca729fd94f88e9b1f36c57f1ccf6a06`. Each matched on both hosts. The GGUF (`322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482`) and pack (`059bb62d2e6d863b32ac47208d29da7492618b2fa33015a3ee0ad6fe2a9c4d54`) also matched. Host `cargo test -p paddock-engine -p paddock-runner -p paddock-dist --offline`, `cargo check --all-targets --offline`, and `git diff --check` exited 0 after the event fix.

CUDA event probes distinguish span enqueue, decode embedding-kernel enqueue, and completed decode tick. The worker initially failed to record a new finisher event after span rows, so its probe read the previous completed event; it now records a rank-1 lane event after every span, including spans without a finishing chunk. On the rebuilt two-Spark run, both ranks reported the prefill finisher event pending after the decode embedding kernel was enqueued (10/10 observed ticks on each rank), but complete after the whole decode forward (0/10 still pending). This proves a CUDA enqueue overlap window on both ranks without interpreting a TCP ACK as a GPU fence; it does not yet prove final-result and state parity. Repeating with TCP_NODELAY on both control sockets made no difference, so that experiment was reverted. Logs: `~/.hermes/cache/scratch/phase10-overlap-long-eager-{head,worker}.log` (the label is a scratch-harness filename; the run used `PADDOCK_TP_OVERLAP_PROBE=1`).

A separate no-probe live-pipe run with the same long-prompt harness exited 0 on both ranks. Greedy pair, reuse categorical and page-boundary HTTP text matched, but the categorical-arrival continuation diverged. With `PADDOCK_TP_TRACE=1` the cause is localized: a declined mixed-spec attempt in the quiet path consumed an extra categorical uniform before falling through to `TpMixed`. At position 8 the quiet mixed tick used `u=0.53156984` and returned ID 0; the overlap pipe used `u=0.9613289` and returned ID 11. Repeating the quiet run with the existing `PADDOCK_NO_MIXED_SPEC=1` switch restored `u=0.9613289` and ID 11 at position 8. The subsequent inputs, plans, and IDs through position 16 matched in both rank-0 traces (`~/.hermes/cache/scratch/phase10-overlap-trace-head.log` and `phase10-overlap-long-eager-head.log`). This comparison isolates a pre-existing declined-spec RNG consumption, not a demonstrated lane-logit discrepancy; speculation was not changed. HTTP text and this partial trace are not substitutes for the complete reset-eager and active-cancellation gates.

A direct two-rank reset-eager oracle now lives at `crates/paddock-engine/examples/qwen35_tp_overlap.rs`. Its release executable SHA-256 `f8a37e4a386a391e4f5028f93dd08f99cb934f05fbaf669e8921e713fa4bea50` matched on both Sparks; both ranks exited 0 (`~/.hermes/cache/scratch/phase10-direct-{head,worker}.log`). With explicit `Categorical { inv_t: 1.25, u: 0.37 }` plans, an arriving 20-token prefill while slot 0 decodes matched eager/reset IDs for both the decode and the prefill finisher, then matched every next-row logit bit-for-bit after rank-local lane-to-decode promotion, crossing the 16-token KV page. A separate Host finisher compared every final-row logit bit-for-bit; two queued prompts completed in FIFO spans with individual final-logit parity. The probe rejected prefill abort while the span was active, drained the pipe and span before release, then reused the slot. This verifies the direct generator contract with a controlled RNG plan; it does not prove that live scheduler cancellation is processed mid-span, nor that every live request's RNG plan matches the eager scheduler. Host `cargo test -p paddock-engine -p paddock-runner -p paddock-dist --offline`, `cargo check --all-targets --offline`, and `git diff --check` passed after adding the oracle.

This was the remaining gate at the time of the interim probe. The acceptance below supersedes this hold; `TpReady` still acknowledges enqueue, not GPU completion.

## Stage-2 TP=2 overlap acceptance

Production `supports_overlap` now follows the resident device-sampling capability (under the scheduler's existing `PADDOCK_UNIFIED=1` opt-in). The temporary overlap-capability switch and row/plan/ID trace switches were removed. TP graphs, mixed speculation and offload were excluded from this acceptance; the live comparisons set `PADDOCK_NO_MIXED_SPEC=1`. Both ranks still require ordered `TpPrepared` before enqueuing collectives and a single tail `TpReady` per batch-family arm; release/reuse remains behind the mapped-pipe drain, prefill-lane join and NCCL/decode fences.

The direct reset-eager two-rank oracle (`crates/paddock-engine/examples/qwen35_tp_overlap.rs`, release SHA-256 `9515458434ade70eff56e52bb6d47cec9dd17900d711c81a5c61d0af47cce570` on both Sparks) exited 0 on both ranks. In addition to fixed-u categorical decode/finisher ID and complete final/next-row logit parity, its opt-in `PADDOCK_TP_STATE_PROBE=1` asserts on **each** rank after the appropriate GPU/NCCL synchronization: mirrored slot-table/refcount/free shape, the bytes of each slot-owned KV page (physical block allocation order is deliberately normalized), and DeltaNet recurrent/conv F32 bit patterns match reset-eager after span finish and promotion. Before either release it checks that the pipe and span have drained, the prefill event is complete and both compute streams plus NCCL are synchronized; after release it checks the survivor's owned payload is unchanged, the freed slot's table is empty and both lane-local DeltaNet states are zero. Reusing slot 1 alongside slot 0 reproduces the reset-eager rank-local snapshots and every next-row logit. Both ranks logged `probe complete` once and exited 0. Evidence: `~/.hermes/cache/scratch/phase10-direct-{head,worker}.log`, `phase10_run_direct.py`. This is a rank-local GPU readback after the fences, not a claim that a TCP acknowledgement is a GPU fence.

For the active-cancellation gate, the paired two-Spark live runs `phase10-cancel-final-{eager,overlap}-{head,worker}.log` used a streamed 191-token victim, an already-decoding fixed-seed categorical survivor and a subsequent categorical slot reuse. The temporary SSE keepalive/drop trace was used only for this measurement and removed afterwards. In the overlap run, CUDA sequence 29 was enqueued with `lane_done=false` at 16:00:07.188235Z, the span launched at .188606Z, the sequence-30 decode-begin CUDA probe still reported `lane_done=false` at .191218Z, and the established victim SSE receiver dropped on socket RST at .191624Z; the later completion probe ran at .255330Z, mapped pipe drained at .312969Z, and span finished at .317033Z. Thus the client cancellation was bracketed by a pending prefill event and its later completion; the scheduler reclaimed the victim after the required drain rather than reusing its state mid-flight. `phase10_verify_cancel.py cancel-final-eager cancel-final-overlap` passed **54 ordered** slot-0 `(position,input,plan,ID)` records (warmup, survivor and reuse), rather than an unordered multiset or HTTP text alone; both rank processes exited 0 for both runs. The scheduler's in-loop closed-receiver log did not fire because `decode_pipe_begin_slots` blocked until after the GPU event completed; this evidence establishes receiver drop during the flight, not synchronous cancellation processing by the scheduler inside the flight.

Final release runner SHA-256 `34fcf30ea2f9f49580625349ca8915b2bbfa703d4bc98e0318067697f7570480` matched on both Sparks. The final production overlap suite (`PADDOCK_UNIFIED=1 PADDOCK_NO_MIXED_SPEC=1`, `phase10-stage2-final-production-overlap-{head,worker}.log` and `-results.json`) exited 0 on both ranks with **10 launches/10 finishes and 10 mapped-pipe begins**; its six scenario groups, including 191-token arriving prompts, categorical arrival/reuse, cancellation, page crossing and randomized arrivals, matched the controlled sampled-eager run (`phase10-stage2-real-sampled-eager-*`, zero spans) on all returned text/finish/usage fields. The accepted ordinary decode-pipe regression (`phase10-stage2-final-production-pipe-*`) likewise exited 0 on both ranks with **14 begin/14 drain segments and zero spans**, matching `phase10-stage2-final-decode-eager-nospec-*` on all six response groups. The live response comparisons corroborate the direct and ordered trace oracles; they are not themselves token-ID evidence. Checkpoint SHA-256 `322e194ff79741c7baa497c240f677f54b201b0efab44ca8e50f122b39123482` and CUDA pack SHA-256 `059bb62d2e6d863b32ac47208d29da7492618b2fa33015a3ee0ad6fe2a9c4d54` were pinned for the two-Spark fixture. Final host verification: `cargo test -p paddock-engine -p paddock-runner -p paddock-dist --offline --quiet` (430 engine library, 506 runner library, 18 distributed integration tests; optional model/heavy fixture tests self-skipped), `cargo check --all-targets --offline --quiet`, and `git diff --check` all exited 0.
