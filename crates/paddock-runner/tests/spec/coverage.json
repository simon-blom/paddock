{
  "_readme": [
    "Spec-parameter coverage matrix - the honest definition of conformance:",
    "every request parameter the pinned SDKs enumerate has a deliberate,",
    "tested disposition. tests/python/spec_conformance.py enforces it:",
    "a param in the SDK but not here FAILS the gate (new spec params must",
    "get a disposition); a param here but not in the SDK is stale and FAILS.",
    "Dispositions:",
    "  implemented - real behavior, covered by the functional gates",
    "  partial     - accepted for a value subset, honest 400 otherwise",
    "  advisory    - accepted with spec semantics (telemetry/routing hints",
    "                with no server-side effect to have)",
    "  rejected    - honest 400 (explicit check or unknown-field rejection)",
    "The gate probes every rejected/partial param (expects 400) and every",
    "advisory param (expects 200).",
    "",
    "`_pinned` IS the pin, and the gate asserts the installed SDKs match it",
    "before believing a single result. This is not hypothetical: the `python`",
    "tests/sdk_conformance.rs invokes once sat months behind the pinned SDKs,",
    "so the whole suite had been passing against a stale spec revision the",
    "entire time. A stale SDK now fails the gate by name.",
    "Bump: pip install -U openai==X anthropic==Y, edit `_pinned`, re-run."
  ],
  "_pinned": {"openai": "2.53.0", "anthropic": "0.120.2"},
  "chat": {
    "audio": "rejected",
    "frequency_penalty": "implemented",
    "function_call": "implemented",
    "functions": "implemented",
    "logit_bias": "implemented",
    "logprobs": "implemented",
    "max_completion_tokens": "implemented",
    "max_tokens": "implemented",
    "messages": "implemented",
    "metadata": "partial",
    "modalities": "partial",
    "model": "implemented",
    "moderation": "rejected",
    "n": "implemented",
    "parallel_tool_calls": "implemented",
    "prediction": "rejected",
    "presence_penalty": "implemented",
    "prompt_cache_key": "advisory",
    "prompt_cache_options": "rejected",
    "prompt_cache_retention": "rejected",
    "reasoning_effort": "implemented",
    "response_format": "implemented",
    "safety_identifier": "advisory",
    "seed": "implemented",
    "service_tier": "advisory",
    "stop": "implemented",
    "store": "partial",
    "stream": "implemented",
    "stream_options": "implemented",
    "temperature": "implemented",
    "tool_choice": "implemented",
    "tools": "implemented",
    "top_logprobs": "implemented",
    "top_p": "implemented",
    "user": "advisory",
    "verbosity": "advisory",
    "web_search_options": "rejected"
  },
  "completions": {
    "best_of": "implemented",
    "echo": "implemented",
    "frequency_penalty": "implemented",
    "logit_bias": "implemented",
    "logprobs": "implemented",
    "max_tokens": "implemented",
    "model": "implemented",
    "n": "implemented",
    "presence_penalty": "implemented",
    "prompt": "implemented",
    "seed": "implemented",
    "stop": "implemented",
    "stream": "implemented",
    "stream_options": "implemented",
    "suffix": "rejected",
    "temperature": "implemented",
    "top_p": "implemented",
    "user": "advisory"
  },
  "responses": {
    "background": "rejected",
    "context_management": "rejected",
    "conversation": "rejected",
    "include": "rejected",
    "input": "implemented",
    "instructions": "implemented",
    "max_output_tokens": "implemented",
    "max_tool_calls": "implemented",
    "metadata": "partial",
    "model": "implemented",
    "moderation": "rejected",
    "parallel_tool_calls": "implemented",
    "previous_response_id": "partial",
    "prompt": "rejected",
    "prompt_cache_key": "advisory",
    "prompt_cache_options": "rejected",
    "prompt_cache_retention": "rejected",
    "reasoning": "implemented",
    "safety_identifier": "advisory",
    "service_tier": "advisory",
    "store": "partial",
    "stream": "implemented",
    "stream_options": "partial",
    "temperature": "implemented",
    "text": "implemented",
    "tool_choice": "implemented",
    "tools": "implemented",
    "top_logprobs": "implemented",
    "top_p": "implemented",
    "truncation": "implemented",
    "user": "advisory"
  },
  "anthropic_messages": {
    "cache_control": "advisory",
    "container": "rejected",
    "inference_geo": "advisory",
    "max_tokens": "implemented",
    "messages": "implemented",
    "metadata": "advisory",
    "model": "implemented",
    "output_config": "implemented",
    "service_tier": "advisory",
    "stop_sequences": "implemented",
    "stream": "implemented",
    "system": "implemented",
    "temperature": "implemented",
    "thinking": "implemented",
    "tool_choice": "implemented",
    "tools": "implemented",
    "top_k": "implemented",
    "top_p": "implemented",
    "user_profile_id": "advisory"
  },
  "audio_transcriptions": {
    "chunking_strategy": "partial",
    "file": "implemented",
    "include": "partial",
    "keywords": "rejected",
    "known_speaker_names": "rejected",
    "known_speaker_references": "rejected",
    "language": "implemented",
    "languages": "partial",
    "model": "advisory",
    "prompt": "implemented",
    "response_format": "implemented",
    "stream": "implemented",
    "temperature": "implemented",
    "timestamp_granularities": "implemented"
  },
  "images_generations": {
    "background": "implemented",
    "model": "advisory",
    "moderation": "advisory",
    "n": "implemented",
    "output_compression": "implemented",
    "output_format": "implemented",
    "partial_images": "implemented",
    "prompt": "implemented",
    "quality": "partial",
    "response_format": "partial",
    "size": "implemented",
    "stream": "implemented",
    "style": "rejected",
    "user": "advisory"
  },
  "images_edits": {
    "background": "implemented",
    "image": "implemented",
    "input_fidelity": "advisory",
    "mask": "rejected",
    "model": "advisory",
    "n": "implemented",
    "output_compression": "implemented",
    "output_format": "implemented",
    "partial_images": "implemented",
    "prompt": "implemented",
    "quality": "partial",
    "response_format": "partial",
    "size": "implemented",
    "stream": "implemented",
    "user": "advisory"
  },
  "_images_readme": [
    "`/v1/images/generations` is JSON like chat, but it needs a LOADED",
    "image-generation model (Qwen-Image), which no other section does - so it",
    "splits the way audio does: the matrix audit is static and runs on every",
    "invocation, the probes and the functional cases run under --images-only",
    "(sdk_conformance.rs::spec_gate_qwen_image starts the server).",
    "`quality` is PARTIAL by value: auto/low/medium/high pick the step count",
    "(the model's 40 for auto and high, 30, 20); `standard` and `hd` are",
    "DALL-E's and are refused by name. The response echoes the quality the",
    "render actually used - never `auto` - which is what the SDK's literal",
    "expects. `response_format` is PARTIAL the same way: `b64_json` is what",
    "the gpt-image models return and what is served; `url` needs a file host",
    "the runner is not.",
    "`moderation` and `model` are advisory: a runner serves exactly one model,",
    "and there is no content filter here to set a level on - the two spec",
    "values are accepted with nothing to do.",
    "`stream` is the GPT-image stream: `partial_images` (0-3) progressive",
    "previews as `image_generation.partial_image` events - the engine's x0",
    "estimate decoded part way through the render, so a preview is the",
    "picture the render is converging on - then `image_generation.completed`",
    "with the final image and the usage. Data-only events discriminated by",
    "`type`, the shape the SDK's Stream[ImageGenStreamEvent] parses; one",
    "image per streamed request (n > 1 with stream is a 400, as is",
    "partial_images without stream). The gate holds it to the invariant that",
    "matters: the streamed final image is BYTE-IDENTICAL to the oneshot render",
    "of the same seed - a preview must never change the answer.",
    "`size` takes any WIDTHxHEIGHT on the model's grid (a multiple of 32, up",
    "to its ceiling) plus `auto`; the response echoes the size generated, so a",
    "strict client reading the SDK's three-value literal should ask for one of",
    "those three. The gate validates a spec-size response strictly and a",
    "custom-size one by hand for that reason.",
    "`/v1/images/edits` is the GPT-image edit form as multipart: `image` (or",
    "`image[]`, up to ten) plus the generation fields. Every reference is",
    "resized to its own aspect at the output area on the 32-pixel grid - the",
    "pipeline's rule - and the output takes the LAST reference's shape unless",
    "`size` says otherwise; the usage tells picture tokens from text tokens.",
    "The lane needs the model's vision tower (the mmproj companion): without",
    "it the endpoint refuses by name and the functional section reports",
    "itself not run. `mask` is REJECTED: the model edits from the instruction",
    "and the pictures, without a mask. `input_fidelity` is advisory (accepted,",
    "nothing to set - there is no fidelity knob on this model); `quality` and",
    "`response_format` are partial exactly as on generations. Extensions:",
    "seed, steps, guidance, negative_prompt (the same four)."
  ],
  "_audio_readme": [
    "`/v1/audio/transcriptions` is multipart/form-data, not JSON, and it needs",
    "a LOADED ASR model to answer anything - no other section does. So it",
    "splits in two: the matrix audit above (SDK params vs dispositions) is",
    "static and runs on every invocation, including boxes with no whisper",
    "checkpoint, because that is the half that catches a new spec param. The",
    "probes and the functional cases need a server and run under --audio-only",
    "(sdk_conformance.rs::spec_gate_whisper starts one).",
"`timestamp_granularities` is implemented (closed it) but WHICH",
    "values a given runner answers is a per-checkpoint fact, published on",
    "/v1/models: whisper answers both, granite-speech-plus answers `word`",
    "only, everything else neither. The functional half reads that list and",
    "derives its expectations from it, so it runs against any lane and a",
    "wrong advertisement fails on the assertions it selected.",
    "`model` is advisory for the reason it is everywhere here: a runner serves",
    "exactly one model, so the field names a deployment rather than selecting.",
"`stream` is implemented on all three lanes and refuses only",
    "srt/vtt, where a partial document is not a partial answer. The gate holds",
    "it to the invariant that matters: the deltas concatenate to done.text.",
    "`prompt` is implemented on all three lanes but means a different thing on",
    "each: whisper feeds it behind `<|startofprev|>` as a spelling/vocabulary",
"hint, the generative lanes carry it as instruction context.",
    "The gate asserts only what a spec client can rely on - accepted, still a",
    "transcript - plus the refusal of a control token smuggled in as text.",
    "`languages` is PARTIAL, and the split is by VALUE rather than by lane: on",
    "a whisper-family runner the codes become a SOFT PRIOR over the server's",
    "own language detection - biasing, never filtering, so an out-of-set",
    "language that dominates the posterior still wins. That is what separates",
    "it from Azure's LID (which returns a candidate language even when none was",
    "spoken) and from Deepgram's (which silently drops speech in a language you",
    "did not name). A code outside the loaded checkpoint's own map is refused",
    "by name, and so is `languages` on a generative ASR lane, which has no",
    "detection to bias. The 400-probe uses an unknown code precisely because",
    "that refuses on EVERY lane. `language` and `languages` together is a 400",
    "too: they answer the same question two ways, and silently preferring one",
    "would drop a field the caller set."
  ],
  "_nested_readme": [
    "The matrices above audit TOP-LEVEL request params only, which is where",
    "conformance was quietly incomplete: the SDKs also define discriminated",
    "unions for content parts, message roles, tool types and tool_choice, and",
    "an unknown member of one of those used to reach the chat template. What",
    "happened next depended on how that template was written - qwen's tests",
    "`'text' in item` and skipped it, so the content VANISHED with a 200.",
    "Measured: an unknown Responses content part, an unknown chat",
    "role, a chat `custom` tool and every unserved Responses tool type were",
    "all silently accepted. Each now has a disposition here and a probe.",
    "Same vocabulary as above; `implemented` members are covered by the",
    "functional gates, everything else is probed for an honest 400."
  ],
  "_nested": {
    "chat": {
      "content_part": {
        "text": "implemented",
        "image_url": "implemented",
        "file": "implemented",
        "input_audio": "rejected",
        "_unknown": "rejected"
      },
      "role": {
        "system": "implemented",
        "developer": "implemented",
        "user": "implemented",
        "assistant": "implemented",
        "tool": "implemented",
        "function": "implemented",
        "_unknown": "rejected"
      },
      "tool": {"function": "implemented", "custom": "rejected", "_unknown": "rejected"},
      "tool_choice": {
        "none": "implemented",
        "auto": "implemented",
        "required": "implemented",
        "function": "implemented",
        "allowed_tools": "rejected",
        "custom": "rejected"
      }
    },
    "responses": {
      "content_part": {
        "input_text": "implemented",
        "input_image": "implemented",
        "input_file": "implemented",
        "_unknown": "rejected"
      },
      "input_item": {
        "message": "implemented",
        "function_call": "implemented",
        "function_call_output": "implemented",
        "mcp_approval_response": "implemented",
        "item_reference": "rejected",
        "_unknown": "rejected"
      },
      "tool": {
        "function": "implemented",
        "web_search": "implemented",
        "mcp": "implemented",
        "file_search": "rejected",
        "code_interpreter": "rejected",
        "image_generation": "rejected",
        "computer_use_preview": "rejected",
        "local_shell": "rejected",
        "custom": "rejected",
        "_unknown": "rejected"
      }
    },
    "anthropic_messages": {
      "content_block": {
        "text": "implemented",
        "image_base64": "implemented",
        "image_url": "rejected",
        "document": "implemented",
        "tool_use": "implemented",
        "tool_result": "implemented",
        "thinking": "implemented",
        "search_result": "rejected",
        "_unknown": "rejected"
      },
      "tool_choice": {
        "auto": "implemented",
        "none": "implemented",
        "any": "implemented",
        "tool": "implemented"
      },
      "tool": {"input_schema": "implemented", "web_search": "implemented", "_unknown": "rejected"}
    }
  },
  "_extensions": {
    "chat": ["top_k", "min_p", "repeat_penalty", "chat_template_kwargs", "file_metadata", "max_pages", "pdf_mode"],
    "completions": ["top_k", "min_p", "repeat_penalty"],
    "responses": ["top_k", "min_p", "seed", "chat_template_kwargs", "file_metadata", "max_pages", "pdf_mode"],
    "anthropic_messages": ["seed", "min_p", "file_metadata", "max_pages", "pdf_mode"],
    "audio_transcriptions": ["paddock_words", "paddock_verbose", "paddock_guards", "paddock_guard_stats", "paddock_language"],
    "images_generations": ["width", "height", "seed", "steps", "guidance", "negative_prompt"],
    "images_edits": ["seed", "steps", "guidance", "negative_prompt"]
  },
  "_extensions_readme": [
    "Extensions are ours, outside the SDK surface, and the gate holds them to",
    "the same standard: each is exercised so a rename or a quiet removal fails.",
    "The images ones are REQUEST fields, sd.cpp's vocabulary where OpenAI has",
    "none: `width`/`height` as the pair alternative to `size`, `seed` (an",
    "omitted seed is 42, so a request is reproducible by default - the",
    "opposite of OpenAI's, deliberately: same seed, same file, same bytes is",
    "what makes a local render a thing you can bisect), `steps` overriding the",
    "quality's step count, and `guidance` + `negative_prompt` for classifier-",
    "free guidance, which the model ships off (true_cfg_scale 1) and which a",
    "negative prompt without is refused for by name.",
    "`paddock_words` is the odd one - a RESPONSE field carrying per-word text",
    "+ logprob + confidence, not a request param, because OpenAI's word object",
    "carries no probability to put it on. It sits on each verbose_json SEGMENT",
    "on the whisper lane, where the times live, and TOP-LEVEL on the generative",
    "lanes, which have logprobs but no timestamp vocabulary and so no segments",
"to hang anything on. There it is opt-in via include=logprobs,",
    "since carrying logprobs costs the decode its overlap fast path.",
"It retires into `words` when lands real word times.",
    "`paddock_verbose` is the second: OpenAI's transcript.text.done carries",
    "`text` and nothing else about the transcript, so a caller who streamed AND",
    "asked for verbose_json would have to re-send the clip to get its segments.",
    "It holds the exact object the non-streaming verbose_json returns.",
"`paddock_guards` is the third, and the only one that exists",
    "because the spec cannot express the fact at all: an ASR decode can fail",
    "without erroring - whisper writes a fluent sentence over a window of",
    "silence, a model gets stuck repeating one token until the context bound",
    "stops it. We now catch both (whisper.cpp's thresholds, minus its retry",
    "ladder), and under the no-silent-failures principle a decode that was cut",
    "or refused has to SAY so. Each entry is a SPAN - {start, end, reason,",
    "text_dropped, note} plus whichever of no_speech_prob / avg_logprob /",
    "entropy the lane could answer - and the key is ABSENT, never an empty",
    "array, on the clips (every one in a clean battery) where nothing fired.",
    "`text_dropped` separates the two remedies: a no_speech span has a positive",
    "signal its audio held nothing, so the text goes; a cut decode keeps what",
    "it said up to the cut. `text`, `srt` and `vtt` carry no notice - they have",
    "nowhere to put one, the same way they have nowhere to put `duration`.",
    "`paddock_guard_stats` is the fourth, the odd one out twice over: it is the",
    "only extension asked for by a REQUEST field of the same name, and the only",
    "one that exists for tuning rather than for reading. `paddock_guards` says",
    "what fired; this says how close everything came, per window, whether or",
    "not anything fired - entropy, avg_logprob, no_speech_prob,",
    "compression_ratio, the stop reason, and the thresholds THIS build applies",
    "so a sweep never has to hardcode our constants. It exists because task",
    "#163 could otherwise only measure that nothing tripped, not whether the",
    "margin was 0.79 nats or 8. It is also the honest end of a promise",
"`compression_ratio` has been making since  - \"so the caller can",
    "apply their own threshold\" - which a caller cannot do on one number out",
    "of four. Numbers a lane cannot answer are ABSENT, never zero: the",
    "generative families have no no-speech head, and granite's timestamp mode",
    "runs no entropy test to report.",
"`paddock_language` is the fifth, and like `paddock_guards` it",
    "exists because the spec cannot express the fact at all. OpenAI's",
    "`language` is one string with nowhere to say where it came from, how sure",
    "the server was, what it nearly said instead, or whether the finished TEXT",
    "is even in that language. The last one is load-bearing: a speech model",
    "given the wrong language does not mislabel its output, it TRANSLATES, and",
    "the result is fluent and grammatical with no ragged edge to notice. So the",
    "object carries `source` (asked | detected | reported | unknown),",
    "`probability` and `candidates` where a detector produced them, `hints`",
    "(with `outranked` when the caller's candidate set overturned the model's",
    "own answer - a soft prior is allowed to be strong, never invisible), and",
    "`written`: the transcript's own language with `agrees`. A contradiction",
    "ALSO raises a `language_mismatch` decode guard, so a plain-`json` caller",
    "with no verbose body to look inside still learns their transcript may be a",
    "translation rather than a transcription."
  ]
}
